"use strict";
// class Player {
//     // name: string;
//     // age: number;
//     // country: string;
function drawRactangle(options) {
    let w = options.width;
    let l = options.length;
    console.log(w, l);
}
drawRactangle({
    width: 20,
    length: 20,
});
